{
  "name": "dits-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "build:cli": "cargo build --locked --release",
    "test:cli": "cargo test --locked",
    "docs:check": "bash scripts/check-product-truth.sh && node scripts/check-markdown-links.mjs && bash scripts/check-cli-docs.sh",
    "docs:links": "node scripts/check-markdown-links.mjs",
    "bench": "node scripts/bench/run.mjs",
    "bench:comparative": "node benchmarks/comparative/run.mjs --profile ci",
    "bench:comparative:showcase": "node benchmarks/comparative/run.mjs --profile showcase",
    "bench:comparative:test": "node --test benchmarks/comparative/test/*.test.mjs",
    "release": "turbo build && echo 'Ready for release'"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.2.0",
  "engines": {
    "node": ">=20.9.0"
  }
}
