{
  "buildCommand": "test -f package.json && test -f next.config.ts && npm run build || (cd apps/web && npm run build)",
  "installCommand": "npm install --ignore-optional",
  "framework": "nextjs"
}
