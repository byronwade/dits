# dits-wasm (generated)

Generated WebAssembly build of the real Dits engine (`packages/dits-wasm` →
`packages/dits-core`). **Do not edit by hand.**

Regenerate after any change to `dits-core` / `dits-wasm`:

```sh
npm run build:wasm   # from apps/web
```

This output is committed so `next build` needs no Rust toolchain.
