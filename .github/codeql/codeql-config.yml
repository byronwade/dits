name: "Dits CodeQL config"

# Exclude test-only files from security analysis to prevent false positives
# from test credential strings (e.g. "test-password" in keystore tests).
paths-ignore:
  - "**/*_test.rs"
  - "**/*_tests.rs"
  - "**/tests/**"
  - "**/test_*.rs"
